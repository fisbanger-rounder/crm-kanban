{
  "name": "crm-kanban",
  "version": "1.0.0",
  "description": "CRM Kanban - Sales Pipeline",
  "scripts": {
    "build": "node build.mjs",
    "dev": "npx http-server . -p 8090 -c-1 --cors"
  },
  "devDependencies": {
    "terser": "^5.31.0"
  }
}
